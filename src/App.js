import React from "react";
import Keyboard from "./Keyboard/Keyboard";
import { Flex } from "@chakra-ui/layout";

const App = () => {
  return (
    <Flex>
      <Keyboard />
    </Flex>
  );
};

export default App;
